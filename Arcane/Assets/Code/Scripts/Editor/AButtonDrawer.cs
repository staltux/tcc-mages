using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

/*
//[CustomEditor(typeof(AButton))]
public class AButtonDrawer : UnityEditor.UI.ButtonEditor
{
    public override void OnInspectorGUI()
    {

        AButton component = (AButton)target;

        base.OnInspectorGUI();

        EditorGUILayout.PropertyField(new SerializedObject(component).FindProperty("holdTime"));
        EditorGUILayout.PropertyField(new SerializedObject(component).FindProperty("longPressTime"));
        EditorGUILayout.PropertyField(new SerializedObject(component).FindProperty("longPressConsumed"));
        EditorGUILayout.PropertyField(new SerializedObject(component).FindProperty("OnLongPress"));


    }
}
*/